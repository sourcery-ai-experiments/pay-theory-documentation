---
id: create-capture
title: createCapture
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








Create a capture for the passed in authorization ID


```graphql
createCapture(
  merchant_uid: String!
  authorization_id: String!
  amount: Int!
  fee: Int
  send_receipt: Boolean
  receipt_description: String
  allow_reauth: Boolean
  allow_exceeded_amount: Boolean
): Transaction!
```




### Arguments

#### [`merchant_uid`](#)<Bullet />[`String!`](/docs/api/types/scalars/string)  
> 
> 
> 
> 

#### [`authorization_id`](#)<Bullet />[`String!`](/docs/api/types/scalars/string)  
> 
> 
> 
> 

#### [`amount`](#)<Bullet />[`Int!`](/docs/api/types/scalars/int)  
> 
> 
> 
> 

#### [`fee`](#)<Bullet />[`Int`](/docs/api/types/scalars/int)  
> 
> 
> 
> 

#### [`send_receipt`](#)<Bullet />[`Boolean`](/docs/api/types/scalars/boolean)  
> 
> 
> 
> 

#### [`receipt_description`](#)<Bullet />[`String`](/docs/api/types/scalars/string)  
> 
> 
> 
> 

#### [`allow_reauth`](#)<Bullet />[`Boolean`](/docs/api/types/scalars/boolean)  
> 
> 
> 
> 

#### [`allow_exceeded_amount`](#)<Bullet />[`Boolean`](/docs/api/types/scalars/boolean)  
> 
> 
> 
> 

### Type

#### [`Transaction`](/docs/api/types/objects/transaction)  
> 
> 
> Transactions are a data object that can represent a payment, failed or successful, or a refund.
> 



